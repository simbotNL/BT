---
description: Launch GStack Browser — AI-controlled Chromium with the sidebar extension baked in.
---
Invoke the gstack `connect-chrome` skill. Read `skills/connect-chrome/SKILL.md` (relative to this plugin root) and follow its instructions exactly, using the user's prompt arguments below as input.

User arguments: $ARGUMENTS
