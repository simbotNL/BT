---
description: Configure deployment settings for /land-and-deploy.
---
Invoke the gstack `setup-deploy` skill. Read `skills/setup-deploy/SKILL.md` (relative to this plugin root) and follow its instructions exactly, using the user's prompt arguments below as input.

User arguments: $ARGUMENTS
