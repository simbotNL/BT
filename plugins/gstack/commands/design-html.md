---
description: Design finalization: generates production-quality Pretext-native HTML/CSS. (gstack)
---
Invoke the gstack `design-html` skill. Read `skills/design-html/SKILL.md` (relative to this plugin root) and follow its instructions exactly, using the user's prompt arguments below as input.

User arguments: $ARGUMENTS
