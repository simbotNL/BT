---
description: Full safety mode: destructive command warnings + directory-scoped edits. (gstack)
---
Invoke the gstack `guard` skill. Read `skills/guard/SKILL.md` (relative to this plugin root) and follow its instructions exactly, using the user's prompt arguments below as input.

User arguments: $ARGUMENTS
