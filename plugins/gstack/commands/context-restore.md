---
description: Restore working context saved earlier by /context-save. (gstack)
---
Invoke the gstack `context-restore` skill. Read `skills/context-restore/SKILL.md` (relative to this plugin root) and follow its instructions exactly, using the user's prompt arguments below as input.

User arguments: $ARGUMENTS
